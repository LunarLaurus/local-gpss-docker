services:
  local-gpss:
    build: .
    image: local-gpss:latest
    container_name: local-gpss
    restart: unless-stopped
    ports:
      - "13579:8080"
    volumes:
      - local-gpss-data:/app/data

volumes:
  local-gpss-data: {}
