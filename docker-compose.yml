services:
  local-gpss:
    build: .
    image: local-gpss:latest
    container_name: local-gpss
    restart: always
    ports:
      - "13579:13579"
    volumes:
      - local-gpss-data:/app

volumes:
  local-gpss-data: {}
